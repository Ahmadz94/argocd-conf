apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-crmv4
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Ahmadz94/demo-crm-chart.git
    targetRevision: master
    path: .
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  ignoreDifferences:
    - group: networking.k8s.io
      kind: Ingress
      name: demo-crmv4-demo-crm
      jsonPointers:
        - /status
        - /metadata/annotations
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
